#!/bin/sh

curl -O ftp://ftp.eu.openbsd.org/pub/OpenBSD/snapshots/amd64/cd54.iso
yum install syslinux
cp /usr/share/syslinux/memdisk /boot/
	
title "OpenBSD  installation"
	kernel /memdisk iso
	initrd /cd54.iso
        
set record with 'memdisk' as default in grub
